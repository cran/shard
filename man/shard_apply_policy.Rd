% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ergonomics.R
\name{shard_apply_policy}
\alias{shard_apply_policy}
\title{Apply Wrapper Policy}
\usage{
shard_apply_policy(
  auto_share_min_bytes = "1MB",
  max_gather_bytes = "256MB",
  cow = c("deny", "audit", "allow"),
  profile = c("default", "memory", "speed"),
  block_size = "auto",
  backing = c("auto", "mmap", "shm")
)
}
\arguments{
\item{auto_share_min_bytes}{Minimum object size for auto-sharing (default "1MB").}

\item{max_gather_bytes}{Maximum estimated gathered result bytes before refusing
to run (default "256MB").}

\item{cow}{Copy-on-write policy for borrowed inputs. One of \code{"deny"}, \code{"audit"},
or \code{"allow"}. Default \code{"deny"}.}

\item{profile}{Execution profile passed through to \code{\link[=shard_map]{shard_map()}}. One of
\code{"default"}, \code{"memory"}, or \code{"speed"}. Default \code{"default"}.}

\item{block_size}{Shard block size for apply-style workloads. Default \code{"auto"}.}

\item{backing}{Backing type used when auto-sharing (\code{"auto"}, \code{"mmap"}, \code{"shm"}).}
}
\value{
An object of class \code{shard_apply_policy}.
}
\description{
Centralizes safe defaults and guardrails for apply/lapply convenience wrappers.
}
\examples{
cfg <- shard_apply_policy()
cfg
}
